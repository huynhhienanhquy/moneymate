# Optional for local Expo development: the app discovers the Metro host.
# Required for preview/production builds, for example https://api.example.com/api.
EXPO_PUBLIC_API_URL=
EXPO_PUBLIC_EAS_PROJECT_ID=
