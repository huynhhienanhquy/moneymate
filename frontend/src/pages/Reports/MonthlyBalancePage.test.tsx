import { expectPageToRender } from '@/test/pageTest';
import MonthlyBalancePage from './MonthlyBalancePage';
it('renders the monthly balance page', () => expectPageToRender(MonthlyBalancePage, 'Tiết kiệm mỗi tháng'));
